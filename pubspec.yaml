name: acnerase2
description: Acne-trigger ingredient scanner using TFLite and camera
version: 1.0.0+1

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  google_ml_kit: ^0.20.0
  camera: ^0.11.1
  tflite_flutter: ^0.11.0

dev_dependencies:
  flutter_lints: ^5.0.0
  flutter_test:
    sdk: flutter


flutter:
  uses-material-design: true
  assets:
    - assets/model.tflite
    - assets/1.tflite
